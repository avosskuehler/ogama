<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" 
    xmlns:tools="clr-namespace:GTApplication.Tools"
    x:Class="GTApplication.CalibrationUI.CalibrationControl" Width="Auto" Height="Auto" Background="{x:Null}">

    <Grid x:Name="GridMain" HorizontalAlignment="Center" VerticalAlignment="Center">

        <Canvas x:Name="CanvasCalibration" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        
<!--         <tools:DragCanvasControl  x:Name="CanvasCalibration" AllowDrop="True" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        Background="{Binding Path=ColorBackground}"
        <Canvas x:Name="CanvasCalibration"  Background="{Binding Path=ColorBackground}"/>
    -->
    
    </Grid>
</UserControl>
